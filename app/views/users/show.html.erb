<div class="page-header">  <h1>My Account</h1>
  <%= link_to "Delete", "/users/#{@user.id}", :method => "delete" , class: 'btn btn-md btn-primary pull-center' %></td>
  <%= link_to "Edit", edit_user_path(@user.id), class: 'btn btn-md btn-primary pull-center' %>

</div>


<p>
  <strong>Name:</strong> <%= @user.name %>
</p>

<p>
  <strong>Email address on file:</strong> <%= @user.email %>
</p>

<p>
  <strong>Current password:</strong> <%= @user.password %>
</p>
